
const abi2 = [
  // Get pool reserves
  "function getReserves() view returns (uint,uint,uint)",
  
  // Get the totalSupply
  "function totalSupply() view returns (uint)",
  // Get the balance
  "function balanceOf(address) view returns (uint)",
  
  // Get rewardRate(address)
  "function rewardRate(address) view returns (uint)"
	];
   
export default abi2;
